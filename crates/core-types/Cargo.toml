[package]
name = "core-types"
version.workspace = true
edition.workspace = true
publish.workspace = true

# Sdílené datové typy. ŽÁDNÉ závislosti na Windows (SPEC kap. 2.2).
[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
